# API de Autenticación

Esta API se encarga de gestionar el inicio de sesión y la autenticación de usuarios.

## Endpoints

- **POST /login**: Inicia sesión con un usuario y contraseña.
- **POST /signup**: Registra un nuevo usuario.